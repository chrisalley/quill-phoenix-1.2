<h2>Edit page</h2>

<%= render "form.html", changeset: @changeset,
                        action: wiki_page_path(@conn, :update, @wiki, @page) %>

<%= link "Back", to: wiki_page_path(@conn, :index, @wiki) %>
